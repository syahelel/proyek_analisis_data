# proyek_analisis_data
ini adalah proyek analisis data dari dicoding data visualization

# dataset
dataset yang di gunakan dari bike sharing dataset adalah day.csv
dashboard menggunakan data dari data.csv yang mana versi clean dari day.csv

# Setup environment
conda create --name main-ds python=3.9
conda activate main-ds
pip install numpy pandas scipy matplotlib seaborn jupyter streamlit babel

# run streamlit
streamlit run dashboard.py
